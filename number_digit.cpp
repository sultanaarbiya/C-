#include<iostream>
using namespace std;
int main()
{
char ch;
cout<<"enter a character:";
cin>>ch;
if(ch>=0&&ch<=9)
   cout<<"given character is a number";
   else
    cout<<"given character is not  a number";
return 0;
}
